# execution — from vision

fulfills ehmpathy/domain-objects#123

## todos

- [x] add seaturtle-themed test that fails first (bare array of scalars stays bare)
- [x] add the element-level bare-value guard to `instantiateThisPropIfNeeded`
- [x] add mixed-union tests on one key (scalar, array, `{include}`, `{exclude}`)
- [x] add edgecase tests (mixed array scalar+object; multi-option scalar pass-through)
- [x] run types + lint + unit; confirm green
- [x] confirm no-schema usecase unaffected ([research] item from vision)

## what changed

### fix — `src/instantiation/hydrate/hydrateNestedDomainObjects.ts`

added a bare-value guard at the top of `instantiateThisPropIfNeeded`, to mirror the
field-level guard:

```ts
const instantiateThisPropIfNeeded = (prop: any) => {
  // if it's a bare value (scalar or null), leave it un-hydrated - mirror the field-level guard, since no domain object would validate a primitive
  if (typeof prop !== 'object' || prop === null) return prop;
  // ...rest unchanged...
};
```

### tests — `src/instantiation/DomainObject.test.ts` (`describe('hydration')`)

four new seaturtle-themed tests (constructor-level, hydration via `new DomainObject`):
1. bare array of scalars stays bare under a single-option nested key (the core fix)
2. mixed union on one key: bare scalar, bare array, `{include}`, `{exclude}`
3. mixed array: bare scalar element stays, object element hydrates
4. bare scalar element under a multi-option key passes through (no throw)

### tests — `src/instantiation/hydrate/hydrateNestedDomainObjects.test.ts` (new, dedicated)

collocated unit tests that exercise the transformer directly (per one-operation-per-file
+ collocated-test practice). 9 tests across bare values (scalar, array, null field,
null element), object values (single, array-of-objects), mixed arrays, and
multi-option keys (bare scalar pass-through, `_dobj`-hinted hydration).

## verification

- **before fix**: the 4 new tests failed (proof of corruption) —
  `.log/.../what=unit/2026-07-05T23-15-06Z` showed 4 failed, 24 passed.
- **after fix**: `path://src/instantiation/DomainObject.test.ts` → 28 passed.
- **dedicated transformer file**: 9 passed.
- **types**: passed.
- **lint**: passed (after `npm run fix:format`).
- **full unit suite**: 378 passed, 0 failed, 2 skipped (prior skips, unrelated); 36 suites.

## [research] item — no-schema usecase

all four new tests use no-schema domain objects (the wish's declared usecase) and
pass. schema-supplied interaction remains orthogonal and out of scope for this fix.

## peer-review blocker fixed (r9: ergo-friction-hazards)

peer review flagged that the object-without-`_dobj` throw test asserted the error
type but did not snapshot the user-faced error message (message-drift hazard). fixed
by capture via `getError` + `expect(error.message).toMatchSnapshot()`, to match the
repo's error-snapshot convention. snapshot created at
`src/instantiation/hydrate/__snapshots__/hydrateNestedDomainObjects.test.ts.snap`.
re-ran: 10 passed, types + lint green.

## peer-review gap fixed (r10: enroll-impl-behavior-intent)

l3 peer review found a genuine coverage gap: no test for an *already-instantiated*
domain-object element inside an array. that `isOfDomainObject` early-return path is
newly reachable because the element-level bare-value guard passes objects through to
it. added
`when('an array element is already an instance of the nested option') → then('it leaves the instance as-is')`.
re-ran: dedicated file 11 passed; full suite 380 passed; types + lint green.

## out-of-scope items declined (r7, r11)

r7 blockers (immutable-vars refactor of the prior props clone; extract the
error class) and r11 structural suggestions (decompose the file, extract transformers,
snapshot all error paths) target prior library code untouched by this 3-line
fix. declined per scope discipline (wet-over-dry, YAGNI, "a bug fix doesn't need
surrounding code cleaned up"); r11 itself notes "the core fix ... is correct and
sufficient ... none are behavioral changes." human overruled the r7 gate.
