wish =

checkout issue #118 (the latest one issued), the radio task

specifically, we gotta add support for nested readonly attributes

some nested domain literals may contain readonly and mutable attributes

so we gotta follow that attribute.access.chain to know which attributes are readonly vs not, all the way through, from the entity level

the domain literal that the entity composed is stateless, so it cant declare a readonly attribute (especially since its reusable accross different domains)

so it has to come from the entity grain.
