# 5.3 verification — buttonup

> prove nested-readonly works via tests. no contracts (cli/api/sdk) in this change — it is a library-internal capability on `omitReadonly` / `hasReadonly` / `HasReadonly`, so snapshot-for-contract sections are N/A. coverage is explicit-assertion unit tests.

## verification checklist

### behavior coverage (vision → test)

| behavior (from vision / issue #118) | test file | test | status |
|-------------------------------------|-----------|------|--------|
| dot-path readonly dropped (single nested) | `omitReadonly.test.ts` | "drop a deeply nested readonly field while it keeps settable peers" | ✅ |
| flat readonly + nested readonly together | `omitReadonly.test.ts` | "drop both a flat explicit-readonly key and a nested readonly path declared together" | ✅ |
| array of nested → every element | `omitReadonly.test.ts` | "apply a nested readonly path to every element of a nested array" | ✅ |
| deep chain (3 levels) | `omitReadonly.test.ts` | "drop a nested readonly field three levels deep" | ✅ |
| declared-but-unresolved leaf → no-op | `omitReadonly.test.ts` | "treat a declared-but-unresolved nested readonly field as a no-op" | ✅ |
| fail-fast: plain nested object (not in `static nested`) | `omitReadonly.test.ts` | "fail-fast when a nested path traverses a plain object not declared in static nested" | ✅ |
| fail-fast: primitive intermediate | `omitReadonly.test.ts` | "fail-fast when a nested readonly path traverses a primitive value" | ✅ |
| fail-fast: absent property (typo'd path) | `omitReadonly.test.ts` | "fail-fast when a nested readonly path references a property absent from the object" | ✅ |
| schema-optional reconstruct | `omitReadonly.test.ts` | "reconstruct cleanly when nested readonly fields are schema-optional" | ✅ |
| drift correctness (no false drift) | `omitReadonly.test.ts` | "make declared-vs-resolved serialize equal (no false drift) after omit" | ✅ |
| runtime `hasReadonly` nested: defined | `hasReadonly.test.ts` | "return true when the nested readonly path is defined" | ✅ |
| runtime `hasReadonly` nested: undefined | `hasReadonly.test.ts` | "return false when the nested readonly path is undefined" | ✅ |
| runtime `hasReadonly` nested: array (every element) | `hasReadonly.test.ts` | "require the nested readonly path on every element of a nested array" | ✅ |
| type-level `HasReadonly` nested narrow | `HasReadonly.type.test.ts` | "narrow a nested readonly field to required (and keep flat keys required)" | ✅ |
| type-error when nested readonly absent | `HasReadonly.type.test.ts` | "type-error when the nested readonly field is absent" (`@ts-expect-error`) | ✅ |
| type-error when flat metadata absent | `HasReadonly.type.test.ts` | "type-error when flat metadata is absent, even with nested readonly declared" (`@ts-expect-error`) | ✅ |

every promised behavior has a test. no behavior left untested.

### zero skips verified
- [x] no `.skip()` / `.only()` in changed files (`omitReadonly`, `hasReadonly`, `HasReadonly.type` + tests) — grep clean
- [x] no silent credential bypasses (no creds needed; pure logic)
- [x] no prior failures carried forward
- note: 2 prior `it.skip` live in `serde/deserialize.test.ts` (performance benchmarks: "faster if schema skipped", "instant on repeat"). unrelated to this change, not introduced here, out of scope to alter.

### snapshot coverage for contract outputs
- N/A — no cli / api / sdk contract added. this change extends library functions; verification is via explicit assertions on returned domain objects / booleans / types.

### snapshot change rationalization
- N/A — no `.snap` files added or changed.

### tests executed — with proof

| suite | command | result | proof |
|-------|---------|--------|-------|
| types | `rhx git.repo.test --what types` | ✅ | exit 0, "🎉 passed (2s)" |
| format | `rhx git.repo.test --what format` | ✅ | exit 0, "🎉 passed (1s)" |
| lint | `rhx git.repo.test --what lint` | ✅ | exit 0, "🎉 passed (6s)" |
| unit | `rhx git.repo.test --what unit --mode apply --thorough` | ✅ | exit 0, "364 passed, 0 failed, 2 skipped" (the 2 skips = prior deserialize perf benchmarks) |
| integration | n/a | — | pure in-memory logic; no i/o boundary added → no integration scope |
| acceptance | n/a | — | no contract/built-artifact added |

- [x] every applicable test command was run and output observed
- [x] exit codes verified (0 = pass)
- [x] no tests skipped/mocked/faked by this change

### contract output snapshot exhaustiveness
- N/A — no user-faced contract (cli/api/sdk) in this change.

### blockers
- none. peer-review gate (`5.1.execution.from_vision`) passed with 0 blockers after the fresh re-evaluation.
