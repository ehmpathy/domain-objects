# blocker — 5.1.execution.from_vision

## what blocks me

the peer-review guard (`--allow-blockers 0`) reports **8 blockers**, but the count is not converging despite genuine fixes. two distinct problems:

### 1. stale review cache (reviews r5–r8)

on re-run, reviews r5–r8 (maintenance, behavior-hazards, behavior-intent, ergo) complete in **~0.8s each** — impossible for a real LLM "hard" review. they return cached results that still flag issues i have **already fixed** this session:
- `HasReadonly` nested type-narrow (behavior-intent blocker.1) — **implemented** (types pass; type tests prove it)
- deeper fail-fast on absent/typo'd path (behavior-intent blocker.2) — **implemented** (2 new tests)
- `splitReadonlyPaths` mutability (maintenance blocker.1) — **fixed** (immutable reduce)

the rendered `.reviews/*.behavior-intent-coverage.md` still shows my **pre-fix** code snippets and an old timestamp (`21-03-54`), confirming it did not re-evaluate the current source.

### 2. remaining blockers are PRIOR `hasReadonly` design (not introduced by this change)

`hasReadonly.ts` is only in review scope because i touched it for nested support. the residual blockers concern code i did **not** write:
- **`DomainObjectMetadataMustBeDefinedError`** (failhides blocker.1): reviewer wants `UnexpectedCodePathError`. but it already `extends HelpfulError` with full context + an actionable example message — a proper domain HelpfulError. changing it **breaks** the test `expect(...).toThrow(DomainObjectMetadataMustBeDefinedError)`. i believe this is a **false positive**.
- **`hasReadonly` throws vs returns `false`** (behavior-hazards): deliberate prior type-guard design (throws on programmer error: non-instantiated object / undeclared metadata). changing to return `false` alters public behavior + breaks tests.
- **type-vs-runtime metadata mismatch** (behavior-hazards / ergo): the `HasReadonly` type falls back to `HasMetadata` defaults when no explicit metadata, while runtime requires explicit metadata. a prior inconsistency, not introduced here.

## what i tried

- read all 8 peer-review outputs; categorized the 12 original blockers.
- fixed every blocker genuinely tied to my change:
  - immutable `splitReadonlyPaths` (reduce, no push/assign)
  - deeper fail-fast: throw when a nested path head is absent from the object (typo'd path) or descends into an absent/primitive value; `null` stays a legitimate absent subtree. added typo-path + primitive + 3-level-deep tests.
  - upgraded the prior plain `new Error` in `hasReadonly` → `UnexpectedCodePathError` with `{ obj }` context.
  - **implemented the `HasReadonly` nested type-narrow** via an intersection overlay `T & DeepRequiredAtPaths<T, paths>` (an intersection is provably a subtype of `T`, so it keeps the `obj is HasReadonly<TDobj>` type-guard contract that my earlier `Omit<T,head> & {…}` attempt broke). rewrote the type tests to assert the narrow (positive + 2 `@ts-expect-error`).
- verified: `types` ✅, `unit` ✅ (364 passed, 2 prior skips), `format` ✅, `lint` ✅.
- re-ran `--as arrived` twice; blockers went 12 → 8, then stuck at 8 due to the stale r5–r8 cache.
- did **not** try `--as passed` (the gate blocks it; and i should not bypass a real review gate without your direction).

## what i need

a decision on the two issues above:

1. **the prior `hasReadonly` design blockers** — should i:
   - **(a)** leave them (my recommendation — out of scope, risky, and `DomainObjectMetadataMustBeDefinedError` is arguably correct), and you accept/override the gate for these, or
   - **(b)** rewrite the prior `hasReadonly` error/return design too (broader change: alters public behavior, updates its tests)?

2. **the stale r5–r8 review cache** — it is counting already-fixed issues as blockers. i cannot read or clear the sealed `.route/` cache files. could you either clear the review cache so r5–r8 re-evaluate the current source, or confirm whether the gate threshold can be adjusted given the fixes are in place?

once you decide, i can proceed immediately.

---

## wisher decision (2026-06-28)

**"keep extant design"** → option (a). i will **not** modify the prior `hasReadonly` design (throws-vs-returns, `DomainObjectMetadataMustBeDefinedError`, type/runtime metadata fallback). those residual blockers are intentional prior design, now explicitly accepted by the wisher.

**implication for the gate**: the peer-review threshold is `--allow-blockers 0`. with the prior design kept, the automated judge will still count those prior-design rule-flags (plus the stale-cache ones). a passing count therefore needs one of:
- the review cache cleared so r5–r8 re-evaluate the current source (drops the already-fixed narrow / fail-fast / mutability blockers), and/or
- the gate threshold raised / overridden for the accepted prior-design flags.

both are outside what i can do from here (the `.route/` cache + guard config are sealed to me). i await wisher action on the gate.
