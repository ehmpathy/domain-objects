# blocker — 5.3.verification

## what blocks me 🪘

same wall as the execution stone: the verification stone's judge is **green** and all 10 substantive
peer reviewers **approved**, but the stone cannot pass because the **`repo-rules` reviewer (r1)
malfunctions**, and a fix for that malfunction requires a **human-only `--as overruled`**.

### the exact state (iteration i001 of the verification stone)

- **judge.1 → ✓ allowed** at the strictest threshold (`--allow-blockers 0 --allow-nitpicks 0`).
- **r2–r11 → all approved, every one 0 blockers / 0 nitpicks**:
  - l1: ergo-contract-snapshots, mech-external-contracts, ergo-acceptance-journey-coverage,
    ergo-snapshot-visual-blemishes, mech-given-when-then, mech-test-intent, mech-test-scope-purity.
  - l3 deep-dives: enroll-verif-snapshot-coverage, enroll-verif-snapshot-blemishes,
    enroll-verif-test-intent — all clean.
- **r1 `repo-rules` → ✋ constraint**: the reviewer skill errors before it can review a single file,
  because its glob matches no files:
  ```
  ✋ --rules glob found nada
     ├─ rules: .agent/repo=.this/**/rule.*.md
  ✋ BadRequestError: --rules glob was ineffective
  ```
  `domain-objects` carries no `.this`-local rule set by design (it governs by the `ehmpathy/mechanic` +
  `ehmpathy/architect` rules), so this reviewer yields no verdict — a harness misconfiguration, not a
  critique of the work. this is the identical malfunction the human already overruled on the execution
  stone.

## what i tried 🪘

- **all 11 self-reviews promised** — each a genuine review. three real gaps found + fixed in code on the
  self-review pass:
  - `DomainObjectKind` was unreachable by any test → imported + bound in `getContract.test.ts`.
  - the published type's `@example` modeled an unexplained `as`-cast → added a boundary-read note.
  - the readme consumer example diverged from that jsdoc → aligned both discovery surfaces.
- **`--as passed`** → ran the full peer-review suite; every substantive reviewer approved, judge passed.
- **`--as overruled`** — a driver may not run it (`✗ only humans can overrule`).
- **a fix to the reviewer** — out of scope + wrong: it would demand a made-up `.agent/repo=.this/**`
  rule set purely to satisfy the glob, which this behavior does not own.

verification proof: types / format / lint green; unit `465 passed, 0 failed, 2 skipped` (the 2 skips are
prior, out-of-scope, flaky wall-clock benchmarks in `deserialize.test.ts`, untouched by this feature).

## what i need 🪘

**a human to overrule the malfunction of the repo-rules reviewer** so the verification stone can pass:

```
rhx route.stone.set --stone 5.3.verification --route .behavior/v2026_07_17.fix-contract-kind --as overruled
```

this is safe: the judge passed at the strictest threshold and every substantive reviewer approved with
0 blockers / 0 nitpicks — the sole item under overrule is a reviewer that could not run because the
repo has no `.this` rules to check. this mirrors the overrule the human already granted for the
execution stone.
