# 5.3 verification — buttonup gate

## .what shipped

two additive upgrades to the `.contract` pragma (domain-objects@0.32.x):
- **ask 1** — published `DomainObjectPragma` + `DomainObjectKind` types (one type, optional keys).
- **ask 2** — `getContract` stamps the real `kind` (`entity | literal | event | object`) from the
  class marker symbols, via a new pure `getKind` transformer.

## behavior coverage (from vision usecases + edgecases)

this is a library-surface (sdk) feature — the "journeys" are the `.contract` output variants and the
`getKind` classification cases the vision's edgecase table demands. each has a concrete assertion; each
contract variant also carries a committed json-schema snapshot.

| behavior (vision edgecase / usecase) | test file | assertion | snapshot? | status |
|---|---|---|---|---|
| entity → `kind: 'entity'` (full keys) | getContract.test.ts | ✓ pragma + `kind` | ✓ | ✅ |
| entity nested-in-parent | getContract.test.ts | ✓ | ✓ | ✅ |
| literal → `kind: 'literal'` | getContract.test.ts | ✓ | ✓ | ✅ |
| literal WITH `static primary` (Sandbar) → still `'literal'` | getContract.test.ts | ✓ `kind==='literal'` | ✓ | ✅ |
| array-nested dobj | getContract.test.ts | ✓ | ✓ | ✅ |
| plain `DomainObject` w/ schema (Seafoam) → `'object'` | getContract.test.ts + DomainObject.test.ts | ✓ `kind==='object'` | ✓ | ✅ |
| `DomainEvent` (WaveObservedEvent) → `'event'` | getContract.test.ts | ✓ `kind==='event'` | ✓ | ✅ |
| 2-level subclass (GreenSeaturtle) → inherited `'entity'` | getContract.test.ts | ✓ `kind==='entity'` | ✓ | ✅ |
| `DomainEntity` WITHOUT `static primary` (Tidepool) → `'entity'`, primary undefined | getContract.test.ts | ✓ `kind==='entity'` + primary undefined | ✓ | ✅ |
| omitted fields optional in pragma (unique/alias/nested absent) | getContract.test.ts | ✓ type-conformance (full + minimal) | n/a | ✅ |
| `getKind` unit: entity | getKind.test.ts | ✓ | n/a | ✅ |
| `getKind` unit: literal | getKind.test.ts | ✓ | n/a | ✅ |
| `getKind` unit: event | getKind.test.ts | ✓ | n/a | ✅ |
| `getKind` unit: base-object fallback (Seafoam) | getKind.test.ts | ✓ `'object'` | n/a | ✅ |
| `getKind` unit: base-object WITH keys (Driftwood) → `'object'` | getKind.test.ts | ✓ marker-not-key-driven | n/a | ✅ |
| `getKind` unit: 2-level subclass inheritance | getKind.test.ts | ✓ | n/a | ✅ |
| `DomainObjectPragma`/`DomainObjectKind` exported from barrel | getContract.test.ts (imports from `@src/index`) | ✓ reachability | n/a | ✅ |

no behavior gap: every vision edgecase + both asks have a test.

## zero skips verified

- [x] no `.skip()`/`.only()` in **any file this feature touches** (getContract, getKind,
      DomainObject, DomainObjectPragma tests) — grep-verified.
- [x] 2 `.skip()` live in `src/manipulation/serde/deserialize.test.ts` (lines 681, 774) — **prior/extant**,
      zero diff vs `main`, from PR #114 (2026-04-10). they are timing/perf benchmarks
      (`should be faster if schema is skipped`, `should be instant on repeat attempts`),
      deliberately skipped as flaky-by-nature (`rule.forbid.time-assumptions`), and unrelated to the
      contract-kind feature. touching them would violate `rule.require.review-test-changes`
      (do not alter unrelated behavior). **justified out-of-scope skips, not coverage gaps.**
- [x] no silent credential bypasses (feature has no external deps — pure library surface).
- [x] no prior failures carried forward.

## snapshot coverage for contract outputs

the public contract is `Dobj.contract` → its json-schema (via `z.toJSONSchema()`). every output
variant has a committed snapshot so reviewers vibecheck the pragma without code execution.

| contract | output variants | snapshot file | status |
|---|---|---|---|
| `Dobj.contract` json-schema | entity, nested-entity, literal, literal-with-primary, array-nested, plain-object, event, 2-level-subclass, entity-without-primary | src/manipulation/__snapshots__/getContract.test.ts.snap | ✅ |
| base `DomainObject.contract` (kind: object) | plain-object-with-keys | src/instantiation/__snapshots__/DomainObject.test.ts.snap | ✅ |

- [x] every contract-output variant is exercised (positive + edge: literal-with-primary, entity-without-primary, plain-object).
- [x] snapshots demonstrate actual json-schema output including the new `kind` field, not just "it ran".

## snapshot change rationalization

| snap file | change type | intended? | rationale |
|---|---|---|---|
| src/manipulation/__snapshots__/getContract.test.ts.snap | added + modified | yes | +94 lines: new variants (event, 2-level, entity-without-primary) added; extant variants gain the new `kind` field in the pragma. this is exactly ask 2 — every `.contract` output now carries `kind`. the additive field is the deliverable, not a regression. |
| src/instantiation/__snapshots__/DomainObject.test.ts.snap | modified | yes | the base-`DomainObject` `.contract` test gains `kind: 'object'` in its pragma — the vision's decided fallback for a plain dobj with a schema but no subclass marker. |

- [x] every `.snap` change reviewed.
- [x] intended changes have clear rationale (the `kind` field is the feature).
- [x] no accidental changes.

## tests executed — with proof

| test suite | command run | result | proof |
|---|---|---|---|
| types | `rhx git.repo.test --what types` | ✓ | 🎉 passed (6s), exit 0, no errors |
| format | `rhx git.repo.test --what format` | ✓ | 🎉 passed (1s), exit 0, no errors |
| lint | `rhx git.repo.test --what lint` | ✓ | 🎉 passed (6s), exit 0, no errors |
| unit | `rhx git.repo.test --what unit --mode apply --thorough` | ✓ | 🎉 passed (4s), exit 0, **465 passed, 0 failed, 2 skipped** (41 suites); the 2 skips are the prior perf benchmarks above |
| integration | n/a | — | feature has no integration surface (pure library transform + type; no i/o, no external deps) |
| acceptance | n/a | — | no built-artifact contract (library export, not a cli/api/service) |

- [x] every applicable test command was run and its output observed.
- [x] exact exit code verified (0 = pass).
- [x] no tests skipped/mocked/faked by this feature.

**note on integration/acceptance**: per `rule.require.test-coverage-by-grain`, `getKind` is a pure
transformer (unit) and `.contract`/`getContract` is a pure library surface with no i/o boundary — so
unit + snapshot coverage is the correct and complete grain. there is no communicator, orchestrator,
or served contract to integration/acceptance-test.

## contract output snapshot exhaustiveness

| contract type | contract | positive snapped? | negative/edge snapped? |
|---|---|---|---|
| sdk (type) | `DomainObjectPragma` / `DomainObjectKind` | ✓ (type-conformance tests: full via SeaturtleSurfboard, minimal via Seafoam) | ✓ (optional-key minimal shape) |
| sdk (getter) | `Dobj.contract` json-schema | ✓ (all 4 kinds) | ✓ (literal-with-primary, entity-without-primary, plain-object edges) |

- [x] every kind value (`entity`/`literal`/`event`/`object`) has a snapshot.
- [x] both misclassification-direction edges from the vision (literal-with-primary, entity-without-primary) have dedicated snapshots.

## blockers

- none.

## summary

both wish asks shipped, faithful to the vision. all applicable gates green with cited proof
(types/format/lint + 465 unit tests). the only skips in the repo are prior, out-of-scope perf
benchmarks. zero omissions in this feature's coverage.
