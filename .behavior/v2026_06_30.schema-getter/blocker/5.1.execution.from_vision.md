# blocker · 5.1.execution.from_vision

## what blocks me

the `--as arrived` signal kicks off an automated peer-review pass. every peer
reviewer (r1 through r9) fails with the same root cause:

```
BrainChoiceNotFoundError: brain not found: fireworks/deepseek/v4-flash
```

the route's peer reviewers are wired to the `fireworks/deepseek/v4-flash` brain,
which is **not provisioned in this environment**. the available brains are:

- anthropic/claude/{opus,haiku,sonnet,code,code/opus,code/haiku,code/sonnet}
- xai/grok/{code-fast-1,3,3-mini,4,4-fast-*,4.1-fast-*}

no fireworks/deepseek brain is registered. this is an infra/config malfunction
in the review **toolset**, not a finding against the feature code.

trace origin: `node_modules/.../rhachet/dist/domain.operations/context/genContextBrain.js:108`
review files: `.behavior/v2026_06_30.schema-getter/.reviews/peer/5.1.execution.from_vision._.review.i1.3aeb295ed3f1769f54.r{1..9}._.given.by_peer.*.md`

## what i tried

- completed all **8/8 execution self-reviews** (promised) before the arrived signal
- ran the full local gate set, all green:
  - types ✅
  - format ✅ (auto-fixed one line-wrap in getContract.test.ts)
  - lint ✅
  - unit ✅ — **401 passed**, 2 skipped (getContract: 11 cases incl. 2 json-schema snapshots)
- sent `--as arrived` → peer pass launched → r1-r9 all `💥 malfunction`
- read the r1 review file to confirm the cause is the absent brain, identical across all reviewers
- confirmed this is not fixable by edits to the feature: the failure is in brain
  resolution, before any review logic runs

## what i need

one of:

1. **point the peer reviewers at an available brain** — change the route's peer-reviewer
   brain config from `fireworks/deepseek/v4-flash` to an available one (e.g.
   `anthropic/claude/code/sonnet`), then i can re-send `--as arrived`.
2. **provision the fireworks brain** — add the `fireworksai` brain provider creds so
   `fireworks/deepseek/v4-flash` resolves, then re-send `--as arrived`.
3. **waive the automated peer pass** — if peer review is not required here, authorize
   `--as passed` to advance the route past execution.

the feature itself is done and verified; only the automated peer-review step is blocked.
