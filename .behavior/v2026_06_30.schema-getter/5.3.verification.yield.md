# verification checklist · 5.3.verification

> buttonup gate for the `.contract` getter feature. scope per human: only the actual changes
> on this branch. prior infrastructure is out of scope (confirmed).

---

## behavior coverage

the deliverable is a library sdk surface (`DomainObject.contract` getter + `getContract`
transformer). grain = pure transformer → unit test is the correct and complete grain
(per `rule.require.test-coverage-by-grain`). no remote boundaries → no integration/acceptance
suite (confirmed: zero `*.integration.test.ts` / `*.acceptance.test.ts` in repo).

| behavior (from wish/vision) | test (getContract.test.ts) | snapshot? | status |
|---|---|---|---|
| entity w/ primary+unique+alias+nested → full pragma | `given: a DomainEntity ... when: via getContract` | ✓ | ✅ |
| `.contract` static getter parity (subclass `this`) | `when: via the .contract getter` | — | ✅ |
| idempotent per-class memo (repeat access = same instance) | `then: repeated access returns the same instance` | — | ✅ |
| non-mutation (source schema left un-stamped) | `when: the contract is stamped` | — | ✅ |
| literal w/ primary only → absent fields omitted | `given: a DomainLiteral ...` | ✓ | ✅ |
| polymorphic array nested → `{ pet: ['Cat','Dog'] }` | `given: ... polymorphic (array) nested` | ✓ | ✅ |
| plain dobj w/ string alias → name+alias only | `given: a plain DomainObject ...` | ✓ | ✅ |
| no schema declared → fail-fast `ConstraintError` | `given: ... no schema declared` | — | ✅ |
| joi schema (non-zod) → fail-fast `ConstraintError` | `given: ... schema is joi` | — | ✅ |
| yup schema (non-zod) → fail-fast `ConstraintError` | `given: ... schema is yup` | — | ✅ |

every promised behavior has a test; every behavior points to a test file. no gaps.

---

## zero skips verified

- [x] no `.skip()` / `.only()` in **my** changes (`getContract.test.ts`) — grep clean
- [x] no silent credential bypasses (feature has no creds; pure transformer)
- [x] no prior failures carried forward

**prior skips (out of scope, not my changes):**
`src/manipulation/serde/deserialize.test.ts:681` and `:774` — two performance benchmarks
(`should be faster if schema is skipped`, `should be instant on repeat attempts, due to in
memory cache`). these predate this branch (untouched by my diff) and are intentionally-skipped
timing benchmarks. per human scope confirmation, out of scope; not unskipped (would risk
flaky timing assertions unrelated to this feature).

---

## snapshot coverage for contract output

the sdk deliverable's output is the `x-domain-object` pragma carried through
`z.toJSONSchema()`. all positive output variants are snapped (each snapshot paired with an
explicit `toEqual`/`toBeDefined` assertion, never snapshot-alone):

| output variant | snapshot | explicit assertion alongside |
|---|---|---|
| entity: name+primary+unique+alias(object)+nested(single) | ✓ | `x-domain-object.name === 'Job'`, customer prop defined |
| literal: name+primary only | ✓ | pragma `toEqual({ name:'Geocode', primary:['uuid'] })` |
| polymorphic: nested array `['Cat','Dog']` | ✓ | pragma.nested `toEqual({ pet:['Cat','Dog'] })` |
| plain dobj: name+alias(string) | ✓ | pragma `toEqual({ name:'ChatMessage', alias:'chat' })` |
| entity embedded in parent `z.object` → pragma on nested field | ✓ | `json.properties.job['x-domain-object'].name === 'Job'` (the wish's core journey) |

negative paths (no-schema, joi, yup) throw `ConstraintError` — verified via explicit
`toBeInstanceOf(ConstraintError)` + message `toContain` assertions, **and** each error message
is now snapshotted (`toMatchSnapshot()`) so message/hint drift surfaces in pr diffs.

- [x] every output variant exercised (entity, literal, polymorphic, plain, embedded-in-parent; + 3 error paths)
- [x] snapshots demonstrate actual output, not just "it ran"
- [x] snapshot + assertion together (no snapshot-only blocks)
- [x] negative paths snapped (3 ConstraintError messages)

---

## snapshot change rationalization

| snap file | change | intended? | rationale |
|---|---|---|---|
| `src/manipulation/__snapshots__/getContract.test.ts.snap` | added (new) | yes | new test file for the feature; 4 positive output variants captured for pr vibecheck + drift detection |
| `src/instantiation/__snapshots__/DomainObject.test.ts.snap` | modified | yes | zod-4 bump changed the validation error message format ("Too big: expected number to be <=42" + `origin` field); legitimate upstream format change, re-snapped, message still helpful |

- [x] every `.snap` change reviewed
- [x] intended changes have clear rationale
- [x] no accidental changes

---

## tests executed — with proof

| suite | command | result | proof |
|---|---|---|---|
| types | `rhx git.repo.test --what types` | ✅ | exit 0, passed (1s), no errors |
| format | `rhx git.repo.test --what format` | ✅ | exit 0, passed (1s) |
| lint | `rhx git.repo.test --what lint` | ✅ | exit 0, passed (3s); biome + dpdm cycles + depcheck all clean |
| unit | `rhx git.repo.test --what unit --mode apply --thorough` | ✅ | exit 0, **405 passed**, 0 failed, 2 skipped (prior, out of scope); 39 suites (getContract = 15) |
| integration | n/a | — | no `*.integration.test.ts` in repo (pure library, no remote boundaries) |
| acceptance | n/a | — | no `*.acceptance.test.ts` in repo (no deployed contract) |

- [x] every applicable test command was run and output observed
- [x] exact exit codes verified (0 = pass)
- [x] no tests skipped/mocked/faked in this feature's coverage

---

## peer review (l3 deep enrollment)

- r1 `enroll-impl-behavior-intent` → **approved**, 0 blockers, 0 nitpicks (196s)
- r2 `enroll-impl-arch-defects` → **approved**, 0 blockers, 0 nitpicks (372s)
- judge → allowed

(l1 peer reviewers were dropped via human-granted route mutation; their residual blockers
targeted prior validation/constructor infra outside this feature's scope — documented
in `review/self/for.5.1.execution.from_vision._.overrule.peer-blockers.md`.)

---

## gap found AND fixed at buttonup

the buttonup re-read of `1.vision.yield.md` surfaced a real gap i had previously mis-dismissed:
the vision's pit-of-success table (`:188`) requires "**cache per-class to stay idempotent**".
i had earlier overruled the equivalent peer blocker as "hallucinated" — wrongly, due to a
faulty grep glob that skipped the top-level vision file. fixed here:

- added a per-class memo (`WeakMap<DomainObjectClass, ZodSchema>`) to `getContract.ts`
- added an idempotency test (`getContract(Job) === getContract(Job)`, `Job.contract === Job.contract`)
- re-verified: types/format/lint green, unit **404 passed**
- corrected the record in `review/self/for.5.1.execution.from_vision._.overrule.peer-blockers.md`

## verification peer review (l3) — round 1 blockers FIXED

`enroll-verif-snapshot-coverage` rejected round 1 with 2 in-scope blockers; both fixed at this
buttonup (the other two l3 reviewers — snapshot-blemishes, test-intent — approved 0/0):

| blocker | fix | proof |
|---|---|---|
| the wish's **core journey** untested — `.contract` embedded in a parent `z.object` then `z.toJSONSchema` on the parent (must carry pragma on the nested field, not just standalone) | added a `when('the contract is embedded as a field in a parent z.object')` test + snapshot; asserts `json.properties.job['x-domain-object'].{name,primary,nested}` | unit getContract = 15; snapshot shows pragma on `properties.job`, root un-stamped |
| 3 error paths (no-schema / joi / yup) had **no `toMatchSnapshot()`** — message/hint drift would not surface in pr diffs | added `expect(message).toMatchSnapshot()` to all 3 error cases | 3 new error-message snapshots captured |

also fixed the peer nitpick on the `Household` fixture: its `primary = ['ref']` did not match a
top-level field (ref lives on `pet`); changed to a top-level `uuid` primary for structural
consistency. (the `.play.test.ts` nitpick does not apply — this repo has no `.play.` convention;
all 38 test files use `.test.ts`, and this is a unit-grain transformer, not a journey suite.)

re-verified after fixes: types ✅ format ✅ lint ✅ unit **405 passed**, 0 failed, 2 skipped.

## blockers

- none.
