 wish =
  - given
    - primary keys are typically database generated metadata => usually optional
  - then
    - RefByPrimary should make it clear that all optional attributes are required on the reference

we should have unit tests that cover that behavior and prove it at the boundaries
