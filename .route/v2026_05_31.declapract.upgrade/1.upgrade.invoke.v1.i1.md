# 1.upgrade.invoke — deliverable

## status

arrived — src cycles fixed, tests pass. blocked only by third-party joi cycles.

## summary

- declapract packages upgraded: `0.13.11` → `0.13.21`, `0.47.10` → `0.47.70`
- roles re-linked successfully
- format/lint fixes applied
- src circular dependencies **fixed** (all 4 eliminated)
- third-party (joi) cycles detected — cannot be fixed by this repo
- types: ✅ pass
- unit tests: ✅ 305 tests pass
- biome: ✅ pass
- cycles: ❌ fails (joi only — no src cycles)

## blocker: joi cycles in node_modules

the new `test:lint:cycles` check detects circular dependencies in the joi validation library (v17.4.0). these are internal to joi and cannot be fixed by this repo.

### cycles detected

```
1) joi/lib/index.js -> joi/lib/cache.js -> joi/lib/common.js -> joi/lib/schemas.js
2) joi/lib/common.js -> joi/lib/messages.js -> joi/lib/template.js
3-7) (additional joi internal cycles)
```

### src cycles fixed

the following src circular dependencies were eliminated:

| cycle | fix |
|-------|-----|
| `index.ts -> deserialize.ts` | changed to direct import from DomainObject.ts |
| `RefByPrimary.ts <-> refByPrimary.ts` | extracted RefByPrimary.type.ts |
| `RefByUnique.ts <-> refByUnique.ts` | extracted RefByUnique.type.ts |
| `Ref.ts -> Ref.type.ts -> RefByUnique.ts` | types now import from reference/ |

new files created:
- `src/reference/RefByPrimary.type.ts`
- `src/reference/RefByUnique.type.ts`
- `src/instantiation/markers.ts`
- `src/instantiation/DomainObjectShape.ts`

### handoff to infrastructure

the `--exclude '^$'` pattern in dpdm intentionally checks node_modules per `rule.forbid.dpdm-exclude-change`. however, many npm packages (joi, etc.) have internal cycles that work fine at runtime.

**options:**
1. upgrade joi to newer version (if cycles resolved)
2. replace joi with zod (major change, no cycles)
3. reconsider lint config — allow-list known stable packages
4. skip lint:cycles check until infrastructure addresses

---

## warning: absent variable `reviewers.users`

```
⚠️ broken practice:provision-github
  > User input error. Variable was declared in file contents but its value was not defined: 'reviewers.users'
```

### root cause

the `practice:provision-github` in `declapract-typescript-ehmpathy` now declares a new variable `reviewers.users` at line 137 of `src/practices/provision-github/best-practice/provision/github.repo/resources.ts`:

```ts
const envProductionOnElse = DeclaredGithubEnvironment.as({
  repo,
  name: 'production-on-else',
  reviewers: { users: @declapract{variable.reviewers.users}, teams: null },
  // ...
});
```

this configures required reviewers for production deployments from non-main branches.

### repos checked

| repo | has `reviewers.users`? |
|------|------------------------|
| declapract-typescript-ehmpathy | no |
| sql-dao-generator | no |
| type-fns | no |

none of the checked repos have this variable defined.

### handoff to infrastructure

this is an infrastructure defect in `declapract-typescript-ehmpathy`:
- a new required variable was introduced without migration path
- extant repos don't have a default value
- variable should either have a sensible default or be optional

**options:**
1. make `reviewers.users` optional with default `null` (no reviewers)
2. provide migration guidance in release notes
3. add `reviewers.users: ['uladkasach']` to all ehmpathy repos

## next steps

stone can proceed — the warning doesn't block the upgrade itself. continue to `2.detect.hazards` for test verification.
