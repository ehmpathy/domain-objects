# 2.detect.hazards — deliverable

## status

complete — hazards identified and documented.

## summary

reviewed all changes in this branch. the following hazards require attention.

---

## test hazards

### 1. jest env var changes (integration + acceptance)

**what changed:**
- `STAGE` env var renamed to `CONFIG`
- `I_KNOW_WHAT_IM_DOING` renamed to `I_KNOW_THE_RISKS`

**why it may break:**
- extant commands or docs referencing `STAGE=test` will fail
- guard rail check for non-test environments changed

**verification:**
```sh
rhx git.repo.test --what integration --mode apply
rhx git.repo.test --what acceptance --against local --env test --mode apply
```

### 2. apikeys config removed from jest.integration.env.ts

**what changed:**
- removed check for `.agent/repo=.this/role=any/skills/use.apikeys.json`
- keyrack.source() now handles credentials

**why it may break:**
- tests may behave differently with new credential flow
- keyrack.source() requires keyrack to be initialized

**verification:**
- integration tests should pass with keyrack unlocked
- `rhx keyrack unlock --owner ehmpath --env test`

---

## cicd hazards

### 3. new test sharding workflow

**what changed:**
- added `enshard` job with `test-shards-setup` action
- added `test-shards-omit` placeholder job
- test jobs now depend on `[install, test-shards-omit]`
- expects `jest.integration.shards.jsonc` and `jest.acceptance.shards.jsonc` files

**why it may break:**
- if shard config files are absent, workflow may fail
- new job dependency graph may cause unexpected parallel execution

**verification:**
- push to branch and check github actions
- verify all test jobs complete

### 4. build step added to test:types

**what changed:**
- `npm run build` now runs before `npm run test:types` in CI

**why it may break:**
- build failures will block type checking
- longer CI time

**verification:**
```sh
npm run build && npm run test:types
```

---

## path hazards

### 5. deleted rhachet.use.ts

**what changed:**
- `rhachet.use.ts` removed from repo root

**why it may break:**
- any commands or imports referencing this file will fail

**verification:**
- check if any imports reference `./rhachet.use.ts` — none found in src/

### 6. deleted use.apikeys.sh

**what changed:**
- `.agent/repo=.this/role=any/skills/use.apikeys.sh` deleted

**why it may break:**
- references to `source .agent/.../use.apikeys.sh` will fail
- keyrack now manages credentials instead

**verification:**
- use `rhx keyrack unlock --owner ehmpath --env test` instead

---

## config hazards

### 7. new test:lint:cycles command with node_modules checking (BLOCKER)

**what changed:**
- added `dpdm` dependency
- added `test:lint:cycles` command: `dpdm --no-warning --no-tree --exit-code circular:1 --exclude '^$' 'src/**/*.ts'`
- `--exclude '^$'` pattern matches empty string only, so node_modules are checked

**why it may break:**
- third-party packages with internal cycles will fail lint
- current blocker: joi has 7 internal cycles

**verification:**
```sh
npm run test:lint:cycles
```

**status:** BLOCKED — joi cycles cannot be fixed. see `1.upgrade.invoke.v1.i1.md` for handoff.

---

## cycle fix changes (src/)

### 8. new type files for cycle breaking

**what changed:**
- added `src/reference/RefByPrimary.type.ts`
- added `src/reference/RefByUnique.type.ts`
- added `src/instantiation/DomainObjectShape.ts`
- added `src/instantiation/markers.ts`
- import paths reorganized to break cycles

**why it may break:**
- if type exports don't match original signatures, downstream will break
- marker symbols need to be consistent for instanceof checks

**verification:**
```sh
rhx git.repo.test --what types
rhx git.repo.test --what unit --mode apply
```

**status:** ✅ verified — types pass, 305 unit tests pass

---

## dependency hazards

### 9. package version bumps

**what changed:**
- `declapract`: 0.13.11 → 0.13.21
- `declapract-typescript-ehmpathy`: 0.47.10 → 0.47.70
- `rhachet`: 1.39.11 → 1.41.15
- `test-fns`: 1.4.2 → 1.15.8
- various rhachet-roles packages

**why it may break:**
- breaking changes in dependencies could affect behavior
- version jumps are significant

**verification:**
```sh
rhx git.repo.test --what unit --mode apply
rhx git.repo.test --what integration --mode apply
```

**status:** ✅ unit tests pass

---

## hazard summary

| hazard | severity | status |
|--------|----------|--------|
| jest env var rename | medium | verify in integration |
| apikeys config removed | low | keyrack replaces |
| test sharding workflow | medium | verify in CI |
| build before types | low | expected |
| rhachet.use.ts deleted | low | no references |
| use.apikeys.sh deleted | low | keyrack replaces |
| lint:cycles node_modules | **blocker** | joi cycles |
| cycle fix type files | low | ✅ verified |
| package bumps | low | ✅ unit tests pass |

## next steps

the main blocker is `test:lint:cycles` failing on joi. this is documented in `1.upgrade.invoke.v1.i1.md`.

proceed to `3.1.repair.test.defects` to run integration tests and verify no other regressions.
