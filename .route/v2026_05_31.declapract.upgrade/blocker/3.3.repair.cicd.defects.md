# blocker: 3.3.repair.cicd.defects

## what blocks you

cannot commit and push without commit permission from human.

the stone instructions say to run:
```sh
rhx git.commit.uses allow
```

this command requires human invocation — mechanics cannot grant themselves permission.

## what you tried

1. verified types pass: `rhx git.repo.test --what types` — passed
2. asked human to run `rhx git.commit.uses allow` — no response yet
3. waited through multiple session stops

## what you need

human to run:
```sh
rhx git.commit.uses allow
```

then i can proceed with:
```sh
printf 'fix(practs): bump to latest best\n\n- upgraded declapract packages\n- applied latest practices' | rhx git.commit.set -m @stdin --mode apply --push
```
