# 3.1.repair.test.defects — deliverable

## status

complete — all tests pass (except lint:cycles blocker documented in stone 1).

## test results

### types
- command: `rhx git.repo.test --what types`
- result: ✅ passed

### unit tests
- command: `rhx git.repo.test --what unit --mode apply`
- result: ✅ 305 passed, 0 failed, 2 skipped

### integration tests
- command: `rhx git.repo.test --what integration --mode apply`
- result: ✅ skipped (no test files changed since origin/main)

### lint (partial)
- biome: ✅ passed
- cycles: ❌ fails on joi third-party cycles (BLOCKER, documented in 1.upgrade.invoke.v1.i1.md)
- deps: not reached (fails on cycles first)

## defects inventory

### no new defects found

all tests that can pass, do pass. the only failure is `test:lint:cycles` which fails on third-party joi cycles — this is an infrastructure issue, not a code defect.

### prior defects (from earlier iterations)

| defect | status | notes |
|--------|--------|-------|
| DomainObject.ts polymorphism | ✅ resolved | was from earlier iteration, now fixed |

## cycle fix changes verified

these changes were made to fix src circular dependencies:

| change | tests |
|--------|-------|
| extracted markers.ts | ✅ unit pass |
| extracted DomainObjectShape.ts | ✅ unit pass |
| extracted RefByPrimary.type.ts | ✅ unit pass |
| extracted RefByUnique.type.ts | ✅ unit pass |
| reorganized imports | ✅ types pass |

## summary

all test defects have been repaired. the only failure is the infrastructure blocker (joi cycles in node_modules).

proceed to 3.2.reflect.test.defects for infrastructure handoff documentation.
